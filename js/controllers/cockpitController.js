angular.module("nativeIP").controller("cockpitController", function ($scope, peers, serverInfoAPI, $interval, $filter) {
  /*var loginDetails = {
    realm: 'asterisk.org',
    impi: 'webrtc_client',
    impu: 'sip:webrtc_client@172.16.0.67',
    password: 'webrtc_client',
    websocket_proxy_url: 'wss://172.16.0.67:8089/ws',
    outbound_proxy_url: '',
  };

  sipml.init(loginDetails);

  $scope.call = function (){
    sipml.call(200).then(() => {
      console.log('console'+response);
    });
  };*/
});